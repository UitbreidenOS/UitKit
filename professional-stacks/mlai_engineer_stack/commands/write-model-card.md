# write-model-card

## Purpose
Generate a model card document for ML/AI models with standard sections and metadata.

## Usage
```
/write-model-card [options]
```

## Options
- `--model`: Model name/identifier
- `--output`: Output file path
- `--format`: Card format (markdown, pdf)

## Implementation
[Stub — to be implemented]
