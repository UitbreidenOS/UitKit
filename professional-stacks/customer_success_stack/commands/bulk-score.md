# Command: /bulk-score

Run health scoring across a cohort of accounts.

## Syntax

```
/bulk-score --cohort=<tag> [--output=json|csv]
```

## Output

Ranked list of accounts by health score, with trend indicators.

## Example

```
/bulk-score --cohort=enterprise --output=csv
```
